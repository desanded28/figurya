[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Don't override startCommand — the Dockerfile CMD uses shell form to expand $PORT
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
